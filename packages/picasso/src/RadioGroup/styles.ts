import { createStyles } from '@material-ui/core/styles'

export default () =>
  createStyles({
    horizontal: {
      flexDirection: 'row'
    },

    labelWithRightSpacing: {
      marginRight: '1em'
    }
  })
