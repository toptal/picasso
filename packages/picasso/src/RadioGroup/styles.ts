import { createStyles } from '@material-ui/core/styles'

export default () =>
  createStyles({
    grid: {
      marginTop: 0,
      marginBottom: 0
    },

    gridItem: {
      lineHeight: 1,
      paddingTop: '0 !important',
      paddingBottom: '0 !important'
    }
  })
