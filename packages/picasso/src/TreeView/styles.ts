import { createStyles } from '@material-ui/core/styles'

export default () =>
  createStyles({
    root: {
      width: '100%',
      height: '100%'
    },
    svg: {
      width: '100%',
      height: '100%'
    }
  })
