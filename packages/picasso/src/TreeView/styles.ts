import createStyles from '@mui/styles/createStyles';

export default () =>
  createStyles({
    root: {
      width: '100%',
      height: '100%',
    },
    svg: {
      width: '100%',
      height: '100%',
    },
  })
