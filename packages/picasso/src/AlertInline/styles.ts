import createStyles from '@mui/styles/createStyles';

export default () =>
  createStyles({
    root: {
      fontSize: '1rem',
      lineHeight: '1.375em',
    },
    iconWrapper: {
      height: '1.25em',
    },
  })
