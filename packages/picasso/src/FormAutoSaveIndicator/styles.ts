import createStyles from '@mui/styles/createStyles';

export default () =>
  createStyles({
    root: {
      visibility: 'hidden',
      marginTop: '0.25em',
      marginBottom: '0.25em',
    },
    visible: {
      visibility: 'visible',
    },
  })
