import createStyles from '@mui/styles/createStyles';

export default () =>
  createStyles({
    root: {
      position: 'absolute',
      right: '1.5rem',
      top: '1.5rem',
    },
    rootCloseable: {
      right: '4rem',
    },
  })
