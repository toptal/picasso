import { createStyles } from '@material-ui/core/styles'

export default () =>
  createStyles({
    root: {
      position: 'absolute',
      right: '2em',
      top: '2em'
    },
    rootCloseable: {
      right: '4em'
    }
  })
