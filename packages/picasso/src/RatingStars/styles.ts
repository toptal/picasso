import createStyles from '@mui/styles/createStyles'

export default () =>
  createStyles({
    radio: {
      display: 'none'
    },
    label: {
      marginRight: '.125rem'
    },
    clickableLabel: {
      cursor: 'pointer'
    }
  })
