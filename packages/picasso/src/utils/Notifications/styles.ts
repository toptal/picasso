import createStyles from '@mui/styles/createStyles';

export default () =>
  createStyles({
    formNotification: {
      width: '20em',
      maxWidth: '100%',
    },
    generalNotification: {
      width: '27.5em',
      maxWidth: '100%',
    },
  })
