/* eslint-disable import/no-extraneous-dependencies */
import { createStyles } from '@material-ui/core/styles'

export default () =>
  createStyles({
    root: {
      flexShrink: 0,
      display: 'block',
      overflow: 'hidden',
    },
  })
