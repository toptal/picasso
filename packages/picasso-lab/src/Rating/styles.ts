import { createStyles } from '@material-ui/core/styles'

export default () =>
  createStyles({
    radio: {
      display: 'none'
    },
    label: {
      marginRight: '.125rem'
    },
    clickableLabel: {
      cursor: 'pointer'
    }
  })
