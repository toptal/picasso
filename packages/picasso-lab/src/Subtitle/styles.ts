import { createStyles } from '@material-ui/core/styles'

export default () =>
  createStyles({
    root: {},
    topMargin: {
      marginTop: '2rem',
      marginBottom: '1.5rem'
    },
    main: {}
  })
