{
  "name": "@toptal/picasso-core",
  "version": "0.0.1",
  "description": "Core components of Picasso",
  "publishConfig": {
    "access": "public"
  },
  "main": "./src/index.ts",
  "scripts": {
    "build:dist": "cross-env NODE_ENV=production ../../bin/build --tsConfig=./tsconfig.json --packageJson=./package.json --outputPackageJson=./ --entry=./build/index.js",
    "postbuild": "git update-index --assume-unchanged ./package.json"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/toptal/picasso.git"
  },
  "author": "Toptal",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/toptal/picasso/issues"
  },
  "homepage": "https://github.com/toptal/picasso/tree/master/packages/picasso-lab#readme",
  "dependencies": {
    "@toptal/picasso-shared": "^0.0.1"
  },
  "devDependencies": {
    "cross-env": "^6.0.3"
  }
}
