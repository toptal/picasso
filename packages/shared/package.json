{
  "name": "@toptal/picasso-shared",
  "version": "0.0.1",
  "description": "Shared types, utils for Picasso internal usage",
  "author": "Toptal",
  "homepage": "https://github.com/toptal/picasso/tree/master/packages/picasso-shared#readme",
  "license": "ISC",
  "main": "./src/index.ts",
  "publishConfig": {
    "access": "public"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/toptal/picasso.git"
  },
  "scripts": {
    "build:dist": "cross-env NODE_ENV=production ../../bin/build --tsConfig=./tsconfig.json --packageJson=./package.json --outputPackageJson=./ --entry=./build/index.js",
    "postbuild": "git update-index --assume-unchanged ./packages.json"
  },
  "bugs": {
    "url": "https://github.com/toptal/picasso/issues"
  }
}
