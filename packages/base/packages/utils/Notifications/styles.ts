import { createStyles } from '@material-ui/core/styles'

export default () =>
  createStyles({
    formNotification: {
      width: '20em',
      maxWidth: '100%',
    },
    generalNotification: {
      width: '27.5em',
      maxWidth: '100%',
    },
  })
