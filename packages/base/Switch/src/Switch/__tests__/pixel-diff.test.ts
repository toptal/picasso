// Local Happo pixel-diff diagnostic — kept as a no-op stub so the empty file
// doesn't break the test suite. The real diagnostic lives in
// migration-runs/2026-05-18/Switch-v1/computed-styles-*.json.
describe('pixel-diff diagnostic', () => {
  it('placeholder', () => {
    expect(true).toBe(true)
  })
})
