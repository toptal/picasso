import { Theme } from '@material-ui/core/styles';
declare const _default: ({ palette }: Theme) => {
    defaultSummary: {
        padding: string;
        color: string;
        fontSize: string;
        fontWeight: number;
    };
    defaultDetails: {
        padding: string;
        lineHeight: string;
        color: string;
    };
    expandIcon: {
        fontSize: string;
        color: string;
    };
};
export default _default;
