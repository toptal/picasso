import { makeStyles } from '@toptal/picasso-provider'

const useStyles = makeStyles()(() => ({
  tabs: {
    marginBottom: '1rem',
  },
  description: {
    fontSize: '0.75em',
    marginTop: '0.5rem',
    marginBottom: '1rem',
  },
}))

export default useStyles
