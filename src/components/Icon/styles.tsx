import { createStyles } from '@material-ui/core'

export default () =>
  createStyles({
    root: {
      fill: 'currentColor',
      display: 'inline-block',
      fontSize: 'inherit',
      height: '1em',
      verticalAlign: '-.125em'
    }
  })
