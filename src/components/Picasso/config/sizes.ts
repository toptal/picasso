export interface Sizes {
  borderWidth: string
  borderRadius: string
  input: {
    height: string
    width: string
    padding: string
  }
}

const sizes = {
  borderWidth: '1px',
  borderRadius: '3px',
  input: {
    height: '2.25em',
    width: '18.75em',
    padding: '0.625em'
  }
}

export default sizes
