---
'@toptal/picasso-form-layout': patch
---

### FormLayout

- Drop `@material-ui/core` peer dependency and lift the `react` peer cap to `>=16.12.0`. Source was already MUI-free; public API unchanged, behavioral parity preserved.
