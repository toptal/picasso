/**
 * THIS FILE WAS GENERATED BY AN LLM (Claude). DO NOT EDIT THIS FILE DIRECTLY.
 * If changes are needed, regenerate this file using the same LLM-assisted workflow.
 */

/* eslint-disable import/no-extraneous-dependencies */
import { readFileSync } from 'fs'
import { resolve, relative, dirname } from 'path'
import glob from 'glob'

const { sync: globSync } = glob

const ROOT = resolve(import.meta.dirname, '../..')

const STORY_PATTERNS = [
  'packages/**/story/index.jsx',
  '.storybook/stories/**/story/index.jsx',
  '.storybook/stories/**/story/index.tsx',
]

/**
 * Classify a story file by scanning its source text for key API calls.
 */
const classifyStory = (source) => {
  if (/\.createDocPage\s*\(/.test(source)) {
    return 'doc-page'
  }

  if (/\.createPage\s*\(/.test(source)) {
    return 'parent'
  }

  if (/connectToPage\s*\(/.test(source)) {
    return 'child'
  }

  if (/createComponentDocs\s*\(/.test(source)) {
    return 'docs-only'
  }

  return 'unknown'
}

/**
 * Discover all story files and classify them.
 */
export const discoverStories = () => {
  const files = STORY_PATTERNS.flatMap((pattern) =>
    globSync(pattern, { cwd: ROOT, absolute: true })
  )

  const unique = [...new Set(files)]

  return unique.map((filePath) => {
    const source = readFileSync(filePath, 'utf-8')
    const type = classifyStory(source)
    const relativePath = relative(ROOT, filePath)

    return { filePath, relativePath, type, source }
  })
}

/**
 * Build a map from story file path to its relative path for cross-reference.
 */
export const buildStoryMap = (stories) => {
  const map = new Map()

  for (const story of stories) {
    const dir = dirname(story.filePath)

    map.set(dir, story)

    const shortPath = story.relativePath.replace(/\/story\/index\.(jsx|tsx)$/, '')

    map.set(shortPath, story)
  }

  return map
}
