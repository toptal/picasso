{
  "packages": ["packages/*", "packages/picasso/src/*"],
  "npmClient": "yarn",
  "version": "independent",
  "command": {
    "publish": {
      "ignoreChanges": ["*.md"],
      "createRelease": "github",
      "yes": true,
      "message": "chore(release): publish [skip ci]"
    }
  },
  "$schema": "node_modules/lerna/schemas/lerna-schema.json"
}
